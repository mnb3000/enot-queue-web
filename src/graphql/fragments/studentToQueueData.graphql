#import "../fragments/studentData.graphql"
#import "../fragments/queueData.graphql"
fragment StudentToQueueData on StudentToQueue {
  studentToQueueId
  status
  student {
    ...StudentData
  }
  queue {
    ...QueueData
  }
}
